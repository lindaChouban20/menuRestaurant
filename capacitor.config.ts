import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manaramenu.app',
  appName: 'menu-restaurant',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
